(()=>{
  // Legacy enhanced field UI intentionally disabled.
  // The product form now keeps the real textareas visible so bilingual
  // translation can read and update the exact fields that are saved.
})();
